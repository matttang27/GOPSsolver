{
  "version": "2.0.0",
  "inputs": [
    {
      "id": "runArgs",
      "type": "promptString",
      "description": "Arguments for solver executable",
      "default": ""
    }
  ],
  "tasks": [
    {
      "label": "solver build",
      "type": "shell",
      "options": {
        "cwd": "${workspaceFolder}\\solver"
      },
      "command": "cmd",
      "args": [
        "/c",
        "cmake --preset vcpkg && cmake --build --preset vcpkg"
      ],
      "problemMatcher": []
    },
    {
      "label": "solver run",
      "type": "shell",
      "options": {
        "cwd": "${workspaceFolder}\\solver"
      },
      "command": "cmd",
      "args": [
        "/c",
        ".\\build\\vcpkg\\Release\\cpp_solver.exe ${input:runArgs}"
      ],
      "problemMatcher": []
    }
  ]
}
